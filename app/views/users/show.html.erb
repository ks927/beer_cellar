<h1><%= @user.name %>'s Profile</h1>

<h3><%= pluralize(@user.beers.count, "beer") %> </h3>

<% if @user == current_user %>
    <h3>Add beer</h3>
    <%= render 'beers/form' %>
<% end %>

<%= render 'beers/table' %>